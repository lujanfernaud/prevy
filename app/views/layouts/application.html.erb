<!DOCTYPE html>
<html>
  <head>
    <title>Private Events</title>
    <%= csrf_meta_tags %>
    <%= render "head_tags" %>
  </head>

  <body class="d-flex flex-column bg-light">
    <%= render "header" %>
    <%= render "flash_messages" %>

    <main class="main-container">
      <%= yield %>
    </main>

    <%= render "footer" unless devise_action? %>
  </body>
</html>
