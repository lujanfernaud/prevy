<!DOCTYPE html>
<html>
  <head>
    <title>Private Events</title>
    <meta name="turbolinks-cache-control" content="no-preview">
    <%= csrf_meta_tags %>
    <%= render "head_tags" %>
  </head>

  <body class="d-flex flex-column bg-light">
    <%= render "header" %>

    <main class="main-container">
      <%= yield %>
    </main>

    <%= render "footer" unless devise_action? || user_settings? %>
  </body>
</html>
