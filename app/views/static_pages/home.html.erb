<%= render "searches/search_bar" %>

<% if logged_in? %>
  <%= render "events", events: @events, events_count: @events_count %>
<% else %>
  <%= render "promo" %>
<% end %>

<%= render "unhidden_groups", groups: @groups %>
